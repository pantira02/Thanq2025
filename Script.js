function filterByService(serviceName) {
  alert("คุณเลือกบริการ: " + serviceName + " (ระบบกรองกำลังพัฒนา)");
  // ในเวอร์ชันเต็มจะใช้ fetch() ดึงเฉพาะผู้รับงานตามบริการ
}

function chatWith(name) {
  alert("กำลังเปิดแชทกับ " + name + " ผ่าน LINE OA");
  // ใส่ลิงก์ LIFF หรือ LINE OA
}
